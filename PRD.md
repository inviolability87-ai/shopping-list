# 쇼핑 리스트 앱 PRD

## 1. 개요
- **제품명**: 쇼핑 리스트 (Shopping List)
- **목적**: 사용자가 구매할 항목을 간단히 관리(추가/삭제/수정/체크)할 수 있는 개인용 웹 앱
- **기술 스택**: Python 3.x, Streamlit
- **대상 사용자**: 개인 사용자 (로컬에서 실행)

## 2. 배경 및 목표
장보기 전 필요한 물건을 빠르게 기록하고, 구매 여부를 체크하며 관리할 수 있는 가벼운 도구가 필요함. 별도 로그인이나 서버 없이 로컬에서 바로 실행 가능한 단일 페이지 앱으로 구현한다.

## 3. 범위 (Scope)

### 포함 (In Scope)
- 아이템 추가
- 아이템 삭제
- 아이템 수정
- 아이템 체크(구매 완료 표시)

### 제외 (Out of Scope)
- 사용자 인증/로그인
- 다중 사용자 동시 편집
- 클라우드 동기화, 알림, 카테고리/태그 분류 (v1 기준 제외)

## 4. 기능 요구사항

| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| F1 | 아이템 추가 | 텍스트 입력 후 버튼/엔터로 리스트에 새 아이템 추가 | High |
| F2 | 아이템 삭제 | 리스트에서 특정 아이템을 개별 삭제 | High |
| F3 | 아이템 수정 | 기존 아이템의 텍스트를 수정 | High |
| F4 | 아이템 체크 | 체크박스로 구매 완료 여부 표시 (완료 항목은 시각적으로 구분, 예: 취소선) | High |
| F5 | 데이터 유지 | 세션 내에서 리스트 상태 유지 (세션 상태 기반, `st.session_state` 사용) | Medium |

> **비고**: 앱 재시작 시 데이터가 유지되는 영속 저장(파일/DB)은 v1 범위에 포함하지 않음. 필요 시 추후 버전에서 JSON 파일 또는 SQLite로 확장 가능.

## 5. 비기능 요구사항
- 단일 파일(`app.py`)로 실행 가능해야 함
- 외부 의존성은 `requirements.txt`에 명시
- 별도 설정 없이 `streamlit run app.py` 명령으로 즉시 실행 가능

## 6. 화면 구성 (UI 개요)
- 상단: 앱 제목
- 입력 영역: 텍스트 입력창 + "추가" 버튼
- 리스트 영역: 각 아이템마다
  - 체크박스 (구매 완료 여부)
  - 아이템 텍스트 (또는 수정용 입력창)
  - 수정 버튼
  - 삭제 버튼
- 완료된 항목은 취소선 또는 회색 처리로 구분

## 7. 산출물 (Deliverables)
- `app.py` — Streamlit 앱 메인 코드
- `requirements.txt` — 의존성 목록 (예: `streamlit`)

## 8. 성공 기준 (Acceptance Criteria)
- 아이템을 추가하면 리스트에 즉시 반영된다
- 아이템을 삭제하면 리스트에서 즉시 제거된다
- 아이템 텍스트를 수정하면 변경된 내용이 리스트에 반영된다
- 체크박스를 클릭하면 완료 상태가 시각적으로 구분되어 표시된다

## 9. 향후 확장 가능성 (Future Considerations)
- 로컬 파일(JSON) 또는 SQLite를 이용한 영속 저장
- 카테고리/수량 필드 추가
- 완료 항목 일괄 삭제 기능
